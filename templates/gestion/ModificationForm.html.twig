{% extends 'base.html.twig' %}{% block stylesheets %}    <link rel="stylesheet" href="{{ asset('style/gestion_modif_animal.css') }}">{% endblock %}{% block body %}    {{ form_start(Modification) }}    {{ form_row(Modification.title, { 'label': 'Titre' }) }}    {{ form_row(Modification.photo) }}{#    {% if photo is not null %}#}{#        <p>Actuellement :</p>#}{#        <img src="{{ asset(photo) }}" height="150px">#}{#    {% endif %}#}    {{ form_end(Modification) }}{% endblock %}