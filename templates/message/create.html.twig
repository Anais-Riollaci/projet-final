{% extends 'base.html.twig' %}

{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block body %}

    <h1>Laissez-nous un témoignage d'adoption !</h1>

    {{ form_start(formContent) }}

    {{ form_widget(formContent) }}

    {# le nom du User ou l'Admin devrait être automatiquement lié au nouveau témoignage #}

{#    <div class="form-group">
        <label for="">Nom du compagnon</label>
        {{ form_widget(formContent.name_animal) }}
    </div>

    <div class="form-group">
        <label for="">Le Témoignage</label>
        {{ form_widget(formContent.content) }}
    </div>

    <div class="form-group">
        <label for="">Une image de votre nouveau compagnon</label>
        {{ form_widget(formContent.friend_image) }}
    </div>
#}

    {{ form_end(formContent) }}

{% endblock %}

